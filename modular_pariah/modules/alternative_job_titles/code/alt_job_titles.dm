/**
 * Alternative job titles have been removed as part of the Shadowrun theming.
 * Each job now has a single canonical title.
 */

/datum/job
	/// The list of alternative job titles people can pick from, null by default.
	var/list/alt_titles = null
